import Leaderboard from '@/components/LeaderBoard';
import React from 'react';

type pageProps = {
    
};

const page:React.FC<pageProps> = () => {
    
    return <div>
        <Leaderboard/>
    </div>
}
export default page;